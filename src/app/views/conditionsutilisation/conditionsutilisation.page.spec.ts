import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ConditionsutilisationPage } from './conditionsutilisation.page';

describe('ConditionsutilisationPage', () => {
  let component: ConditionsutilisationPage;
  let fixture: ComponentFixture<ConditionsutilisationPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ConditionsutilisationPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
